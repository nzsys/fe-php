[server]
host = "0.0.0.0"
port = 8080
workers = 4

[php]
libphp_path = "/opt/homebrew/Cellar/php/8.4.14_1/lib/httpd/modules/libphp.so"
document_root = "/Users/nzsys/Web"
worker_max_requests = 0  # Disable worker restart (prevent channel closure under load)
use_fpm = true
fpm_socket = "127.0.0.1:9000"

[logging]
level = "warn"
format = "json"
output = "stdout"

[metrics]
enable = false
